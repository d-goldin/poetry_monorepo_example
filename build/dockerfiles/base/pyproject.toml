[tool.poetry]
name = "common_deps"
version = "0.1.0"
description = "An umbrella for common depenendcies, to accellerate building of containers"

authors = ["Dima <dgoldin+github@protonmail.ch>"]

[tool.poetry.dependencies]
python = "^3.7"
numpy = "^1.18.1"
pyopenssl = "^19.1.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
